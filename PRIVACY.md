# プライバシーポリシー

**Language**: **日本語** | [English](PRIVACY_en.md) | [繁體中文](PRIVACY_zh-TW.md)

aoiko は **サーバを持たない** 純フロントエンドアプリです。利用者のデータは原則として **端末から外へ出ません**。本書では取得・送信されるデータの内訳を明示します。

## 収集する情報：なし

開発者・配布者は、利用者から以下のいずれも **取得しません**：

- 個人を特定できる情報（氏名・住所・電話・メール）
- 帳簿データ、仕訳、取引履歴
- API キー、認証情報
- 端末情報、IP アドレス
- 利用状況の解析（テレメトリ・アナリティクス）
- クッキー、ローカルストレージへのトラッキング

<!-- only:browser -->
ホスティング先（<https://aoiko.pages.dev> の配信元）の **HTTP アクセスログ** は、そのサービスのポリシーに従って残る可能性があります。これは aoiko 側で制御できません。
<!-- /only -->

## 端末内に保存されるデータ
<!-- only:browser -->

ブラウザの **IndexedDB**（`aoiko` データベース）に以下を保存します：
<!-- /only -->
<!-- only:native -->

アプリの管理領域（`aoiko` データベース）に以下を保存します：
<!-- /only -->

| データ | 保存場所 | 送信先 |
|--------|----------|--------|
| 仕訳、明細、取引先、補助科目 | IndexedDB | 送信なし |
| 固定資産、家事按分ルール | IndexedDB | 送信なし |
| 申告済みスナップショット | IndexedDB | 送信なし |
| Gemini API キー | IndexedDB | 利用者が起動した生成 AI/OCR 機能の際のみ、Gemini API に送信 |
| OpenAI 互換 API キー・baseURL | IndexedDB | 利用者が起動した生成 AI/OCR 機能の際のみ、利用者が指定した baseURL に送信（localhost 指定時は端末外送信なし） |
| 事業者情報（屋号、インボイス番号） | IndexedDB | 送信なし |
| バックアップフォルダのハンドル | IndexedDB | 送信なし |
| インポート履歴（ファイルハッシュ） | IndexedDB | 送信なし |
<!-- only:browser -->

すべて利用者の端末ローカルにのみ存在します。ブラウザのサイトデータ削除を行うと **完全に消失** します。
<!-- /only -->
<!-- only:native -->

すべて利用者の端末ローカルにのみ存在します。アプリをアンインストールする、またはアプリのデータを削除すると **完全に消失** します。
<!-- /only -->

## 端末外へ送信されるデータ

### OCR / 生成 AI エンジン（利用者の任意・BYOK・設定で選択）

利用者が生成 AI 分類・領収書 OCR を **明示的に起動** した場合のみ、選択中のエンジンへ送信されます。送信内容：

- **vision 生成 AI 路（Gemini / OpenAI 互換）**：生成 AI 分類＝CSV 行テキスト（金額・摘要等）＋勘定科目一覧／OCR＝領収書画像（Base64）＋抽出指示プロンプト
- **Tesseract 路（OCR のみ）**：生成 AI 不使用。画像は WASM で端末内処理され外部送信なし。`jpn.traineddata` / `eng.traineddata` も aoiko 自身が配信するため、外部への通信は発生しない
<!-- only:native -->
- **OS 内蔵の文字認識 路（OCR のみ）**：生成 AI 不使用。画像は OS が備える文字認識で端末内処理され外部送信なし。追加データの取得も発生しない
<!-- /only -->
<!-- only:apple -->
- **Apple Intelligence 路（生成 AI 分類・OCR 共通）**：推論は端末内で完結し、画像・テキストとも外部送信なし。追加データの取得も発生しない
<!-- /only -->
<!-- only:browser -->
- **ブラウザ内蔵 AI 路（生成 AI 分類・OCR 共通）**：aoiko からの送信は発生しないが、推論を端末内で行うか外部のサービスで行うかはブラウザの実装が決める（API の仕様はクラウドを使う実装も認めており、端末外に出ないことを aoiko は保証できない）。このエンジンは利用者のブラウザが AI モデルを既に保持している場合にのみ使え、そのモデルの取得を aoiko が行うことはない
<!-- /only -->

| エンジン（設定で選択） | 送信先 | 端末外送信 |
|------|------|------|
| Google Gemini（既定） | `generativelanguage.googleapis.com` | あり（クラウド） |
| OpenAI 互換 / Ollama 等：localhost 指定時 | 端末内（例 `http://localhost:11434`） | **なし** |
| OpenAI 互換 / Ollama 等：リモート指定時 | 利用者が指定したホスト | あり |
| Tesseract（純ローカル WASM OCR） | 画像は端末外に出ない。`jpn.traineddata` / `eng.traineddata` も同梱 | **無し**（外部への通信が発生しない） |
<!-- only:native -->
| OS 内蔵の文字認識 | 画像は端末外に出ない | **無し**（外部への通信が発生しない） |
<!-- /only -->
<!-- only:apple -->
| Apple Intelligence | 画像・テキストは端末外に出ない | **無し**（外部への通信が発生しない） |
<!-- /only -->
<!-- only:browser -->
| ブラウザ内蔵 AI | ブラウザの実装が決める（端末内とは限らない）。使えるのは利用者のブラウザが既に AI モデルを保持している場合のみ（aoiko は取得しない） | aoiko からは無し。**ブラウザが外部へ出すかどうかは aoiko が保証できない** |
<!-- /only -->

<!-- only:browser -->
- 送信は利用者のブラウザから **直接** 行われ、aoiko の管理サーバ（存在しない）は経由しません
<!-- /only -->
<!-- only:native -->
- 送信はアプリから **直接** 行われ、aoiko の管理サーバ（存在しない）は経由しません
<!-- /only -->
- **外部（クラウド）送信エンジン使用時は送信直前に確認ダイアログ** を表示します
- Gemini：送信内容は Google のプライバシーポリシーおよび利用者の API プラン契約に従い、学習利用の有無は契約形態（無料枠 vs 有料枠）で異なります
- ローカル（Ollama 等を localhost で）使用時はデータが端末外に出ません（OCR には vision モデル必須）。端末内で完結するエンジンも同様に送信が発生しません
- Tesseract：生成 AI を使わず WASM OCR テキスト → 確定性ベース抽出（T+13 桁登録番号・日付・合計のみ）。精度は限定的で、店名・品目は推定しません。利用者が必ず人手で確認してください
<!-- only:native -->
- OS 内蔵の文字認識：生成 AI を使わず OS の文字認識テキスト → 確定性ベース抽出（T+13 桁登録番号・日付・合計のみ）。店名・品目は推定しません。利用者が必ず人手で確認してください
<!-- /only -->
<!-- only:apple -->
- Apple Intelligence：推論は端末内で完結し、利用者のデータは外部へ送信されません。この端末が Apple Intelligence に対応している場合にのみ選択肢に出ます
<!-- /only -->
<!-- only:browser -->
- ブラウザ内蔵 AI：aoiko からデータを送信することはありませんが、推論先が端末内かどうかはブラウザの実装によります（仕様はクラウドを使う実装も認めています）。このエンジンが使えるのは利用者のブラウザが既に AI モデルを保持している場合のみで、そのモデルをブラウザがいつ・どのように取得するかは aoiko の管理外です
<!-- /only -->

### バックアップ（利用者の選択）

| 方法 | 送信先 |
|------|--------|
<!-- only:browser -->
| 同期フォルダ（File System Access API・対応ブラウザ） | 利用者が選択した **ローカル** フォルダ |
| OPFS（対応していないブラウザでの後備） | ブラウザ管理の **端末ローカル** ストレージ |
| 手動書き出し | 利用者の「ダウンロード」フォルダ |
<!-- /only -->
<!-- only:native -->
| 同期フォルダ（アプリが 1 つ記憶します） | 利用者が選択した **ローカル** フォルダ |
| 手動書き出し | 利用者が指定した保存先 |
<!-- /only -->

aoiko 側のサーバには **一切** 送信されません。

## クッキー・トラッキング

- aoiko 自身はクッキーを使用しません
- サードパーティ広告・解析タグはありません
<!-- only:browser -->
- リファラ送信はブラウザのデフォルト挙動に依存します
<!-- /only -->

## 法的整合性

- 日本国内利用を想定しています。**個人情報保護法**に基づく「個人情報取扱事業者」要件は、aoiko の開発者・配布者が利用者から個人情報を取得しないため適用外と考えます。
- 利用者自身が aoiko を介して取り扱う他者の個人情報（取引先名・領収書の顧客名等）の管理責任は利用者にあります。
- EU 圏での利用は想定していませんが、GDPR 観点でも収集データなしのためデータ管理者該当性は低いと考えます。

## 変更履歴

本ポリシーは予告なく変更される可能性があります。重要な変更は GitHub の commit log で確認できます。