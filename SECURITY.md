# セキュリティポリシー

**Language**: **日本語** | [English](SECURITY_en.md) | [繁體中文](SECURITY_zh-TW.md)

aoiko は純フロントエンドの BYOK（Bring Your Own Key）アプリです。aoiko 側のサーバは存在せず、帳簿データは利用者の端末に留まります。利用者が明示的に起動した生成 AI 分類・OCR に限り、選択中のエンジンへ内容が送信されます（端末内で完結するエンジンを選んだ場合は送信も発生しません）。本書では既知のリスク・サポート方針・脆弱性報告手順を整理します。

## 正規の配布元

aoiko は以下からのみ公式に配布されます：

- ソースコード：<https://github.com/Lonshaus/aoiko>
- オンライン試用版：<https://aoiko.pages.dev>
<!-- only:apple -->
- App Store（macOS 版・iOS 版）
<!-- /only -->
<!-- only:windows -->
- Microsoft Store（Windows 版）
<!-- /only -->
<!-- only:browser -->

上記以外の場所（不明なサイト、実行ファイル等）から入手した場合は、**API キーや機微情報を入力する前に、必ず上記どちらかで内容を照合してください**。
<!-- /only -->
<!-- only:native -->

ストア以外の場所から入手した場合は、**API キーや機微情報を入力する前に、必ず上記のいずれかで内容を照合してください**。
<!-- /only -->

aoiko は AGPL-3.0 で公開されており誰でも合法的に fork できますが、それは「正規の配布元がどこか」を分かりにくくする悪用（フィッシング・マルウェアの偽装配布等）を排除するものではありません。判断に迷う場合は GitHub リポジトリの commit 履歴・Issue で真正性を確認してください。

## サポート対象バージョン

<!-- only:browser -->
`master` ブランチの最新コミットを反映した公開版（<https://aoiko.pages.dev>）をサポート対象とします。
<!-- /only -->
<!-- only:native -->
各ストアで配布している最新のリリース版をサポート対象とします。旧版のままのご報告は、最新版で再現するかの確認をお願いすることがあります。
<!-- /only -->

## 脆弱性の報告

機密性のある脆弱性報告は **GitHub Security Advisories** をご利用ください。

1. リポジトリの **Security** タブ → **Report a vulnerability**
2. 影響範囲・再現手順・想定される影響を記載
3. パブリック issue では報告しないでください

公開状態の問題（例：誤った勘定科目、UI バグ）は通常の issue で構いません。

返信時間は努力目標として 7 日以内を目指しますが、ボランティアベースのため保証はありません。

## 設計上のセキュリティ前提

### BYOK モデル

<!-- only:browser -->
- 利用者が選択した OCR/AI エンジン（Google Gemini API ／ OpenAI 互換 ／ Tesseract ／ ブラウザ内蔵の AI）の API キー・エンドポイント設定は **利用者自身が登録・自分のブラウザ IndexedDB に保管** します（Tesseract とブラウザ内蔵の AI は鍵も設定も要りません）
<!-- /only -->
<!-- only:apple -->
- 利用者が選択した OCR/AI エンジン（Google Gemini API ／ OpenAI 互換 ／ Tesseract ／ OS 内蔵の文字認識 ／ Apple Intelligence）の API キー・エンドポイント設定は **利用者自身が登録・アプリの管理領域に保管** します（Tesseract・OS 内蔵の文字認識・Apple Intelligence は鍵も設定も要りません）
<!-- /only -->
<!-- only:windows -->
- 利用者が選択した OCR/AI エンジン（Google Gemini API ／ OpenAI 互換 ／ Tesseract ／ OS 内蔵の文字認識）の API キー・エンドポイント設定は **利用者自身が登録・アプリの管理領域に保管** します（Tesseract と OS 内蔵の文字認識は鍵も設定も要りません）
<!-- /only -->
- 開発者・配布者は利用者の API キー・エンドポイント情報を **取得・転送・保管しません**
<!-- only:browser -->
- 外部 API 利用時のリクエストは **利用者のブラウザから直接、選択中のエンドポイントへ送信** されます（プロキシ無し）。端末内で読み取るエンジンを選んだ場合は AI API 送信そのものが発生しません
<!-- /only -->
<!-- only:native -->
- 外部 API 利用時のリクエストは **アプリが選択中のエンドポイントへ直接送信** します（aoiko 側の中継サーバはありません）。端末内で読み取るエンジンを選んだ場合は AI API 送信そのものが発生しません
<!-- /only -->

### ストレージ

- 帳簿データ、API キー、設定はすべて **IndexedDB（端末のローカル）** に保存
<!-- only:browser -->
- バックアップ：同期フォルダ（File System Access API・対応ブラウザ）／ OPFS（対応していないブラウザでの後備）／ 手動書き出し
<!-- /only -->
<!-- only:native -->
- バックアップ：同期フォルダ（アプリが 1 つ記憶します）／ 手動書き出し
<!-- /only -->
- **aoiko の管理サーバへの送信は一切ありません**（aoiko には管理サーバが存在しません）。AI/OCR API 利用時は利用者が設定した外部エンドポイント（Gemini / OpenAI 互換等）にのみ送信されます

## 既知のリスク

### 1. サーバ側監査ログの不在

- 不正アクセス・データ漏洩の **検知手段がありません**
- 端末侵害＝データ漏洩 と等価

### 2. 端末内ストレージの漏洩
<!-- only:browser -->

- 同じ端末の別ユーザ、マルウェア、ブラウザ拡張機能から IndexedDB が読まれる可能性
<!-- /only -->
<!-- only:native -->

- 同じ端末の別ユーザ、マルウェアからアプリの保存領域が読まれる可能性
<!-- /only -->
- 個人情報・取引履歴・API キーがそのまま読まれます
- 業務端末でのご利用、端末暗号化を推奨します

### 3. AI API 送信内容のリスク

- CSV 行・領収書画像は、利用者が選択中のエンジンに従って以下へ送信されます：
  - **Gemini** → `generativelanguage.googleapis.com`（Google のデータ取り扱い方針に従い、学習利用の有無は契約形態による）
  - **OpenAI 互換**（Ollama 等） → 利用者が指定した baseURL。localhost 指定時は端末外送信なし
  - **Tesseract** → 送信なし（WASM で端末内処理。言語データも同梱のため外部への通信は発生しません）
<!-- only:browser -->
  - **ブラウザ内蔵の AI** → aoiko からの送信なし（推論先はブラウザの実装が決め、端末内とは限らない）
<!-- /only -->
<!-- only:native -->
  - **OS 内蔵の文字認識** → 送信なし（端末内処理のみで完結）
<!-- /only -->
<!-- only:apple -->
  - **Apple Intelligence** → 送信なし（推論は端末内で完結）
<!-- /only -->
- 機密度の高いデータの送信前に確認してください（外部エンジン使用時は送信直前に確認ダイアログを表示）
- AI/OCR 機能は **任意起動**（UI ボタン）であり、自動送信はしません

<!-- only:browser -->
### 4. PWA キャッシュ

- 古いビルドが Service Worker にキャッシュされる可能性があります
- バグ修正版の伝播に時間差が出ます
<!-- /only -->

## ハードニング推奨

- 端末のディスク暗号化を有効化
<!-- only:browser -->
- ブラウザのプロファイル分離（業務用と私用を分ける）
- 信用しないブラウザ拡張機能をインストールしない
<!-- /only -->
- 定期的なバックアップ
- 不要になった API キーは Google 側で **必ず失効** させる

## 依存ライブラリの脆弱性

- `npm audit` を CI で実行する予定（未実装）
- 緊急性の高い CVE は反映を急ぎますが、対応は保証されません